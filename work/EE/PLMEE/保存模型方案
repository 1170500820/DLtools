保存模型

首先保存state_dict，
	- 需要在train中制定目录，trainer需要接收working_dir作为参数来推断模型存储位置
然后保存init_params

加载可调用结构

利用init_params加载model，然后导入state_dict
然后需要恰当的输入转换
其实就是需要tokenizer，tokenizer又需要plm_path，实际上这个输入转换过程是需要参数的
所以，得想办法把批量数据处理，也就是dataset部分的代码的参数与这个输入转换融合一下